# El motor criptográfico se publica en GitHub Packages, no en el registro
# público. Hace falta un token con `read:packages` en NPM_TOKEN para instalar.
@projectellysia:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
